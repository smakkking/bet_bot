from EXE_scripts import load_last_data, scan_database, all_bet, find_matches_live, relogin_live

# осуществляет непосредственно ставочный процесс для всех клиентов
# работает круглосуточно

if __name__ == "__main__" :
    #load_last_data.main()
    #client_data = scan_database.main()
    #all_bet.main(client_data)
    #find_matches_live.main()
    relogin_live.main()


