По новому стандарту реализована только группа ExpertMnenie_group!!!!!!!!!

Проблемы :
1) процесс ставки на бк без api очень долго выполняется
2) авторизация на бк
3) Что делать, если пользователь поменял пароль на бк??????
4) В программах не обрабатываются ошибки ВООБЩЕ!!!!!!!!!!!!!
5) протестировать BETSCSGO_betting

6) all_bet.py в бк модулях ввели переменную TAKES_MATHES_LIVE - 0, ссылки на матчи записаны в табице; 1 - иначе
    переделать на обработку этой паременной

7) регистрация проходит, но войти не дает!!! (сука)


!!!!!!!!!!УБЕДИТЕЛЬНАЯ ПРОСЬБА НЕ КОММИТИТЬ КЭШ-ДАННЫЕ!!!!!!!!!!

В шаблонах проверять строки с верзним регистром, воизбежание проблем распознавания(возможно и все данные о матче передавать в верхнем регистре)

Улучшения
1) в бк без API в начале дня получать список всех матчей , сохраняя их в переменной(либо в файле) в json виде -> экономия времени
2) в экземплярах класса Stavka указывать дату и время матча, а может даже и название турнира(взаимодейсвие с п.1)
3) название матча представлять в виде двух команд, а не одной строки