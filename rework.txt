Тут записаны правки в формате
имя_файла
    функция
	правки
	...
	...
	...

+ Думаю нужно переносить main_script в общую
+ Как вариант интеграции ООП - сделать объекты групп и букмекерок(скорее всего так и будет)

Прототип группы:
class Group :
    def __init__(self, url) :
	self.url = url
class GroupName(Group) :
    def parse_bet(...) :
	...	

Нужно создать дерево ставок:
корень - общее название(например - 1-ая карта);
листья- исходы;
И для каждой бк разместить таблицу смещений(этим механизмом как-бы приводим все ставки к общей форме)

parimatch_betting
    bet
    search_bet
	нужно переделать весь функционал, так как вышла новая версия сайта
	она без возможности поиска по названию матча блять...., поэтому исправления пока откладываются